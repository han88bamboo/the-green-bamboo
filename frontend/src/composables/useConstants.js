// Here we maintain a list of which drink type can enable variant/vintage feature 
// on bottle listing and venueprofile menu creation
export const VARIANT_DRNK_TYP = ['Wine', 'Sake', 'Beer'];

// export in group, so we can choose which to make public or stay local at file level
export const CONSTANTS = {
  VARIANT_DRNK_TYP
};